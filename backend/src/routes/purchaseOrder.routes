import { Router } from 'express';

const router = Router();

// Placeholder for purchase order routes
router.get('/', (req, res) => {
  res.status(200).json({
    success: true,
    message: 'This endpoint would return all purchase orders',
  });
});

export default router;